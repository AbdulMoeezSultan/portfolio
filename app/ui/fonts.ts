import { Oswald } from 'next/font/google';
import { Pompiere } from 'next/font/google';

export const oswald = Oswald({subsets: ['latin'], weight: '300'})
export const pompiere = Pompiere({subsets: ['latin'], weight: '400'})